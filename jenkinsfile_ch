pipeline {
    agent any 

    stages {
        stage('Checkout Stage1'){
            steps {
                sh 'pwd'
            }
        }
        stage('Build Stage') {
            steps {
                sh 'cd /etc'
            }
        }
        stage('Starting database rebuild'){
            steps {
                 sh 'pwd'
            }
        }
    }
}
